name: 'build the build warden'

on:
  push:
    branches: [ main, proof-of-concept ]
  pull_request:
    branches: [ main, proof-of-concept ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v4
        with: { go-version: 'v1.25.1', check-latest: true }
      - uses: aquaproj/aqua-installer@v4.0.3
        with: { aqua_version: v2.53.11 }
      - run: ./build.sh lint
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix: 
        os: [ macos-latest, ubuntu-latest ] #TODO: Add windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v4
        with: { go-version: 'v1.25.1', check-latest: true }
      - run: ./build.sh build
      - run: warden --help
