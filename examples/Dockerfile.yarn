FROM node:22-slim

RUN apt-get update && apt-get install -y --no-install-recommends git && \
    rm -rf /var/lib/apt/lists/*
WORKDIR /app
RUN corepack enable && yarn init -y && yarn add lodash@4.17.21
