FROM python:3.12-slim

# Install uv package manager
RUN pip install uv

WORKDIR /app

# Install matplotlib
RUN uv pip install --system matplotlib

# Verify installation
RUN python -c "import matplotlib; print(f'matplotlib {matplotlib.__version__} installed')"
